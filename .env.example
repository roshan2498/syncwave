# Optional — enables in-app YouTube search (https://console.cloud.google.com/apis/credentials)
YOUTUBE_API_KEY=

# Render / production set PORT automatically
# PORT=3001

# Persist SQLite somewhere durable on your host if available
# DATA_DIR=/var/data
